function pg15()
{
    background(51);

    nextButton.show();
    textAlign(CENTER);
    textFont('Oswald');
    fill(255);
    textSize(height / 10);
    textStyle(BOLD);

    text('Many More Applications', width / 2, height / 2);
}
