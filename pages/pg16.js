function pg16()
{
    background(51);

    nextButton.hide();
    textAlign(CENTER);
    textFont('Oswald');
    fill(255);
    textSize(height / 8);
    textStyle(BOLD);

    text('Thank you', width / 2, height / 2);
}
